v2.9.4 — URGENT: FlushPanel override saves are broken, and it is my fault
==========================================================================

UPLOAD FIRST
  flushpanel.html    the fix
  index.html         version stamp only. BUMP sw.js.
  tools/audit-ui.js  check 14, so this cannot happen again

  No rules change. No data change. No app.js change.
  The rules are FINE — the error message blaming them was wrong.


WHAT BROKE
  Your screenshot said "Could not save — check the overrides write rule." The rule was never the
  problem. Look at the bottom of that screenshot: raw JavaScript comment text was rendering as
  visible page content —

      "...and THROWS: 6,595 location ids were OSM-derived and contained..."

  That is my comment. If it is visible on the page, the code around it is not in a script block.

  In Phase 1 I inserted the fsId() helper into flushpanel.html by searching for the first
  occurrence of a comment banner. That anchor matched a position AFTER the final closing script
  tag. So:
     - the browser rendered the comment as page text
     - fsId was undefined inside the module where all 10 call sites live
     - every doc(db,'overrides', fsId(locId)) threw ReferenceError
     - the catch swallowed it and blamed the Firestore rule

  Reports still listed, because that path does not use fsId. Only overrides and tips did — which
  is exactly the symptom you hit.

  This has been live since Phase 1 shipped. Every override you saved since then failed.

FIXED
  fsId now lives inside the module block (bytes 26895-64904), before all 10 uses. Verified by
  locating the enclosing block, not by eyeballing indentation.


AND A SECOND BUG I CREATED WHILE FIXING THE FIRST
  My replacement comment explained the mistake by writing the closing script sequence literally.
  The HTML parser ends a script block at that sequence REGARDLESS of JavaScript context — inside
  a comment, inside a string, anywhere. That would have truncated the module and broken the panel
  far worse than the bug I was fixing.

  Caught because I re-derived the block boundaries after editing instead of trusting the edit.
  The comment now says "closing script tag" in words, and warns the next person.


GUARD — check 14
  Two rules, both from mistakes made today:
    every top-level function must be inside a script block
    no script block may contain a literal closing-script sequence
  Verified: it FAILS on the file as it shipped ("function fsId() is declared outside every script
  block") and passes on the fix. index.html: 49 blocks clean. flushpanel.html: 42 blocks clean.


WHAT I VERIFIED
  fsId inside the module block, before all 10 uses, defined exactly once
  no stray closing-script sequence in any block of either file
  flushpanel ids unique, divs balanced, script tags balanced
  CHAIN_TABLE still 40 rows, still 40 dataset script tags
  14 audit checks pass

WHAT I COULD NOT VERIFY
  The panel in a browser. Please open FlushPanel and save an override on any location — that is
  the exact path that has been failing. If the comment text at the bottom of your screenshot is
  gone, the script block is intact.


WHAT THIS SAYS ABOUT MY EARLIER CHECKING
  I verified that file repeatedly today — ids unique, divs balanced, script tags balanced, all
  Firestore paths normalised. Every one of those passed while the panel was broken, because none
  of them asked whether the code was in a position to RUN. Balanced tags do not mean working
  code. That is the gap check 14 closes.
