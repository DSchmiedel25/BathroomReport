v2.8.5 — coordinate corrections + CI
=====================================

WHAT IS IN THIS BATCH
  cumberland-farms-locations.js   1 coordinate corrected
  circle-k-locations.js           1 coordinate corrected, 2 duplicate records merged away
  index.html                      version stamp only. BUMP sw.js.
  .github/workflows/checks.yml    NEW (this is the v2.8.4 file — include it if you have not
                                  already shipped v2.8.4)

  No app.js, no rules, no functions.

WHAT WAS STILL UNSHIPPED FROM EARLIER
  functions/index.js — I gave you this as index.js separately. Still needs:
      firebase deploy --only functions
  Four fixes are not live without it: the fsId path fix, aggregates.amen tallies, the
  24:01-24:59 hours regex, and the stale-verified-metadata fix.
  If v2.8.4 has not shipped, checks.yml is in this zip too. Nothing else outstanding.


THE CORRECTIONS — all three from your verification
--------------------------------------------------
1. Cumberland Farms, Bridgewater MA
   bridgewater-pleasantst sat on the Main St store's exact coordinate. Moved 3.46 km to
   41.971890, -71.008410 (1001 Pleasant St). I padded your 5 decimals to 6 so the stored
   precision matches what you supplied rather than implying more.

2. Circle K, Franklin TN
   1501 Murfreesboro Rd and 1501 New Hwy 96 W were one store under two road names. Kept
   Murfreesboro Rd per your call, moved it 75m to 35.914000, -86.820580, dropped the alias.

3. Circle K, Jacksonville FL
   3631 Blanding Blvd and the same address "Bldg 2" were one store. Merged; the survivor
   absorbed anything the dropped record had.

Every correction records provenance — coord_prev, coord_corrected, coord_source, and for the
merges, mergedFrom plus altAddress. altAddress matters: the Circle K API returns these stores
under BOTH names, so a future import that does not recognise the alias would re-add them.

  28,089 -> 28,087 records


WHAT I VERIFIED
  Each supplied coordinate sanity-checked before applying: inside a plausible town box, lat/lng
  not transposed, and the move distance sensible (3.46 km for a genuine relocation, 75m for a
  refinement).
  ZERO exact-coordinate groups with different addresses now exist, across all 28,087 records.
  DATA-01 and DATA-07 are both fully closed — DATA-01 had been open since the first audit.
  app.js parses · all 11 audit checks pass · 40 data files load, one global each · SEO generator
  exits 0 with no collisions.

WHAT I COULD NOT VERIFY
  That the coordinates are right. They are yours; I checked they are plausible and consistent,
  not that they are correct. And whether a second Circle K really does exist at Blanding Blvd —
  I took your word and merged.


REMAINING WORK
  Needs the Mac:
    firebase deploy --only functions      (the index.js I sent separately)
    git rm index.js quiktrip-locations.json bathroomreport-nyc-public.zip robots.pdf sw.pdf tools/tools
    curl the two Leaflet plugins          PWA-02
  Worth doing, not started:
    SEO-03  og:image points at /og.png, which does not exist — every one of ~22,000 pages and
            every link you share has a blank preview card. Most visible remaining issue.
    SEO-02  5,750 pages are typed ConvenienceStore when they are rest areas, public restrooms
            or cafes.
  Product decision:
    SEC-01/02  report spam needs App Check, not rules
  Closed won't-fix, with measurements:
    RB-08   marker construction and the viewport scan, both measured and cheap; the previous
            attempt at culling caused the Android races
    SEC-09  the block check fails open deliberately, and the comment says so
